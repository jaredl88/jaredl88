def move_zeros(array):
    arr = array
    count_num = 0  
    for n, i in enumerate(arr):
        if i == 0:
            count_num = count_num + 1 
    arr = list(filter(lambda x: (x != 0), arr))      
    arr.extend([0] * count_num)  
    return arr
